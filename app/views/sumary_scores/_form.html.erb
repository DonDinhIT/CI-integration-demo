<%= form_for(@sumary_score) do |f| %>
  <% if @sumary_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sumary_score.errors.count, "error") %> prohibited this sumary_score from being saved:</h2>

      <ul>
      <% @sumary_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
