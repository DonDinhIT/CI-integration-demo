require 'spec_helper'

describe "SumaryScores" do
  describe "GET /sumary_scores" do
    it "works! (now write some real specs)" do
      get sumary_scores_path
      expect(response.status).to be(200)
    end
  end
end
